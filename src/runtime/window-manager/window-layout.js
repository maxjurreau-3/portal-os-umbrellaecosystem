// ---------------------------------------------------------------------------
// Window Layout Manager — Portal‑OS‑v3
// ---------------------------------------------------------------------------

export const WindowLayout = {
  defaultPosition(id) {
    return {
      x: 80 + Math.random() * 120,
      y: 80 + Math.random() * 120
    };
  }
};
